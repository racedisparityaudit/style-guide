
title: Ethnicity facts and figures style guide

# Build settings
markdown: kramdown
theme: govuk-jekyll-theme

defaults:
  -
    scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default"
